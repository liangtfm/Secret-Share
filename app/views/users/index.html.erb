<h1> Users </h1>

<ul>
<% @users.each do |user| %>

<li>
    <%= link_to user.username, user_url(user) %>

    <% unless user.id == current_user.id  || Friendship.can_friend?(user.id, current_user.id)  %>

      <%= render "friendships/form", user: user %>

    <% end %>
</li>
<% end %>
</ul>